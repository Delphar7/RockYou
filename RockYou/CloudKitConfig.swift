// CloudKitConfig.swift
// RockYou
//
// Single source of truth for the app's CloudKit container identity, shared by
// every CloudKit consumer (household sync, runtime app config).

enum CloudKitConfig {
  static let containerId = "iCloud.com.jtr.RockYou"
}
